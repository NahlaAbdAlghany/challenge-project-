apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: voting-dev
spec:
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: master
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP