apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: result-pvc
  labels:
    app: result
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: rook-cephfs 
  resources:
    requests:
      storage: 1Gi