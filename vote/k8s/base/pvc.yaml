apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vote-pvc
  labels:
    app: vote
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: rook-cephfs 
  resources:
    requests:
      storage: 1Gi